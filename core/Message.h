#pragma once

#include <functional>
#include "api.h"

enum class CORE_API Command {
	NULL_COMMAND,
	CREATE_NODE,
	DELETE_NODE,
	EDIT_NODE,
	GET_NODE,
};

class CORE_API Message {
 public:
 private:
};
